{
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "run-p build:*",
    "build:core": "yarn workspace strencoderkit build",
    "postinstall": "patch-package"
  },
  "devDependencies": {
    "npm-run-all2": "^7.0.2",
    "patch-package": "^8.0.0"
  }
}
